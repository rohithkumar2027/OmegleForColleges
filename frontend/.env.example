# Backend API URL for frontend requests.
# Leave empty to use the current browser host on port 8001.
REACT_APP_BACKEND_URL=

# Expose the React dev server on your LAN for same-WiFi testing.
HOST=0.0.0.0
PORT=3000
